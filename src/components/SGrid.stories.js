import SGrid from './SGrid.vue'

export default {
  title: 'Layout & surfaces/SGrid',
  component: SGrid,
  tags: ['autodocs']
}

export const Default = {
  args: {
    "columns": 2,
    "gap": "1em"
  }
}
