import SPasswordInput from './SPasswordInput.vue'

export default {
  title: 'Form controls/SPasswordInput',
  component: SPasswordInput,
  tags: ['autodocs']
}

export const Default = {
  args: {
    "modelValue": "",
    "placeholder": "Password"
  }
}
