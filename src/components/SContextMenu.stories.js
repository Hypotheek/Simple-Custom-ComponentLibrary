import SContextMenu from './SContextMenu.vue'

export default {
  title: 'Overlays/SContextMenu',
  component: SContextMenu,
  tags: ['autodocs']
}

export const Default = {
  args: {
    "items": []
  }
}
