import SAlert from './SAlert.vue'

export default {
  title: 'Feedback & status/SAlert',
  component: SAlert,
  tags: ['autodocs']
}

export const Default = {
  args: {
    "type": "info",
    "dismissible": false
  }
}
