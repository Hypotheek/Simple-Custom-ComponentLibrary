import SBanner from './SBanner.vue'

export default {
  title: 'Feedback & status/SBanner',
  component: SBanner,
  tags: ['autodocs']
}

export const Default = {
  args: {
    "type": "info",
    "dismissible": true
  }
}
